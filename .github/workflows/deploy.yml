name: Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: 0ssLc9O0e06ZqLA-0o6oTGXNGzA5rW0Ezd_FCUMG
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
